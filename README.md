# booking-system

Final Project: **Booking System** backend (Spring Boot + PostgreSQL + JWT).

If you run the app via `docker compose`, the API is exposed on `http://localhost:8081`
to avoid conflicts with other local services that may already use port `8080`.

Main documentation: see [summary.md](/Users/zolamanbekbolatov/www/java/finalProject/booking-system/summary.md).
